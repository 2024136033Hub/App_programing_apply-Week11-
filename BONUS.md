# BONUS.md — 가산점 신청

**프로젝트:** 수다 — 청각장애인을 위한 한국어 수어(KSL) 번역 플랫폼
**신청 항목:** A (+1), B (+2)

---

## 항목 A — AI Agent / 스킬 / 워크플로우 적극 활용 (+1점)

### 사용한 도구
**Claude Code** (claude-sonnet-4-6) — 기획·설계·구현·디버깅·문서화 전 과정

### 적용한 워크플로우

```
/spec  →  /plan  →  /impl  →  /test  →  /docs
사양      계획      구현       테스트    문서화
```

### 구체적 활용 사례 (절약된 시간 / 향상된 품질)

| 단계 | 활용 내용 | 결과 |
|------|-----------|------|
| 기획 | 비전·요구사항·WBS·위험 문서 자동 생성 | `planning/` 전체 산출물 완성 |
| 설계 | 아키텍처 다이어그램, ADR 3개 작성 | `docs/architecture.md` + ADR 완성 |
| 구현 | FastAPI 인증(JWT), SQLite ORM, React Context API | 로그인·번역기록 기능 완성 |
| 디버깅 | `passlib`↔`bcrypt` 호환 문제 진단 → 직접 `bcrypt` 사용으로 전환 | 500 에러 해결 |
| 디버깅 | Gemini API 429 할당량 초과 → 다중 키 순환 로직 구현 | 안정적 API 호출 |
| 테스트 | pytest 단위테스트 11개 + 통합테스트 1개 자동 작성 | `12 passed` 확인 |
| 문서 | setup.md, deploy.md, testing.md 자동 생성 | 과제 문서 완비 |

### 증빙
- `AGENTS.md` — AI Agent 활용 전체 기록
- `planning/` 디렉토리 — AI와 함께 작성한 기획 산출물
- `backend/tests/test_api.py` — AI가 생성한 테스트 코드

---

## 항목 B — 본인만의 기법 구성 + 설명 (+2점)

### 기법명: 단일 AUTHORING 파일 기반 프로젝트 부트스트랩

**핵심 아이디어:**
`AUTHORING.2024136033.v1.0.md` 파일 하나만 있으면
새 프로젝트에서도 동일한 AI Agent 환경을 5분 안에 재현할 수 있다.

### 구조

```
AUTHORING.2024136033.v1.0.md  ← 이 한 파일로
        │
        ├── AGENTS.md 자동 생성
        ├── 워크플로우 6단계 정의
        ├── 코딩 컨벤션 규칙
        └── 자주 쓰는 프롬프트 패턴
```

### 왜 이렇게 만들었는가
매 프로젝트마다 AI에게 "우리 프로젝트는 이런 구조입니다, 이런 방식으로 작업합니다"를 반복 설명하는 게 비효율적이었다.
AUTHORING 파일 한 장에 모든 맥락을 담아두면, 새 세션을 시작할 때 이 파일만 주면 AI가 즉시 프로젝트 컨텍스트를 파악한다.

### 실제 사용된 흔적
- `AGENTS.md` — AUTHORING에서 정의한 워크플로우가 실제로 적용된 결과물
- `planning/decisions/ADR-*.md` — AUTHORING의 "결정은 ADR로 기록" 규칙 적용
- `docs/` 전체 문서 — AUTHORING의 문서화 컨벤션 적용

### 증빙
- `AUTHORING.2024136033.v1.0.md` (루트 디렉토리)

---

## 항목 C — 최신 LLM Wiki 기반 본인 암묵지 운영 (+1점)

### 위치
`docs/llm-wiki.md`

### 내용
수다 프로젝트를 통해 축적한 LLM/AI Agent 활용 노하우 12개 항목.

| 항목 | 내용 |
|------|------|
| 프로젝트 부트스트랩 패턴 | AUTHORING 단일 파일로 세션 시작 비용 최소화 |
| 디버깅 에러 전달 방법 | 스택 트레이스 원문 전달 → 빠른 원인 파악 |
| API 키 관리 패턴 | 다중 키 순환 로직 설계 원칙 |
| 비동기 에러 처리 | catch 블록에서 에러 숨기지 않기 |
| Promise.allSettled 패턴 | 외부 API 부분 실패 처리 |
| Gemini Vision 멀티모달 호출 | Part.from_text() 명시적 사용 |
| ADR 작성 시점 | 결정 직후 바로 기록 |
| 환경변수 경로 문제 | __file__ 기준 절대 경로 |
| 외부 미디어 서버 차단 대응 | 프록시 + fallback UI |
| AI 생성 코드 검토 원칙 | 보안 관련 코드 반드시 직접 검토 |
| 6단계 개발 워크플로우 | /spec /plan /impl /test /docs /review |
| 프롬프트 작성 팁 | 파일 경로·함수명·에러 메시지 포함 |

### 증빙
- `docs/llm-wiki.md` (12개 항목, 실제 프로젝트 사례 기반)

---

## 요약

| 항목 | 내용 | 가산점 |
|------|------|--------|
| A | Claude Code로 기획~배포 전 과정 AI Agent 활용, 구체 사례 7개 | **+1점** |
| B | AUTHORING 단일 파일 부트스트랩 기법, 새 프로젝트 5분 재현 가능 | **+2점** |
| C | LLM Wiki 암묵지 관리 (docs/llm-wiki.md, 12개 항목) | **+1점** |
| **합계** | | **+4점** |

---

*최종 발표 시 슬라이드 1장으로 어필 예정*
